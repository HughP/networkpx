#!/bin/bash

declare -a cydia
cydia=($CYDIA)

if [[ $1 == remove ]]; then
	iKeyEx-KBMan removeall
	rm /var/mobile/Library/Keyboard/iKeyEx/cache/*
	if [[ ${CYDIA+@} ]]; then
	        eval "echo 'finish:restart' >&${cydia[0]}"
    	fi
	exit 0
fi
